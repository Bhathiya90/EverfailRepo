{% extends '::base.html.twig' %}

{% block body -%}
<div class="grid_9">
    <h1 class="vendor">Vendor List</h1>
</div>
<div id="portlets">
    <div class="column">
        <b>List of Registered Vendors</b>
    <br/>
    <table width="100%" cellpadding="0" cellspacing="0" id="box-table-a" summary="Employee Pay Sheet">
        <tbody>
            <tr>
                <th>{{ vendorName }}</th>
                <td>{{ entity.vendorName }}</td>
            </tr>
            <tr>
                <th>{{ vendorEmail }}</th>
                <td>{{ entity.email }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="news_items">
    <li>
        <a href="{{ path('vendor') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('vendor_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
</ul>
 {{ form(delete_form) }}
    </div>
    </div>
{% endblock %}
